#include <bits/stdc++.h>

void solve() {
    int x, y; std::cin >> x >> y;
    std::cout << std::min(x, y) << '\n';
}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    solve();

    return 0;
}