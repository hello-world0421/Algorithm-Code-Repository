#include <bits/stdc++.h>

void solve() {
    int n; std::cin >> n;
    std::vector<std::vector<int>> dp(n + 1, std::vector<int>(n + 1, 0));

    
}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    solve();

    return 0;
}