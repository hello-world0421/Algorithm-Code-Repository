#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    ll a, b; cin >> a >> b;
    cout << a * b - a - b << '\n';
}

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);

    solve();

    return 0;
}