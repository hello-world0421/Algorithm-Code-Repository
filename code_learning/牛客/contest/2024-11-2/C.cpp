#include <bits/stdc++.h>

void solve() {
    std::string s; std::cin >> s;
    std::string ns = s.substr(0, s.length() - 1);
    std::cerr << s << '\n';
}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    solve();

    return 0;
}