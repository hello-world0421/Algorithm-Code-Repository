#include <bits/stdc++.h>

void solve() {
    std::string s; std::cin >> s;
    std::cout << (s.length() != 1 ? "moon_star" : "nene") << '\n';
}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    solve();

    return 0;
}