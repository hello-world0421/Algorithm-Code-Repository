#include <bits/stdc++.h>

void solve() {
    std::string s; std::cin >> s;
    if (s.length() == 1) {
        std::cout << "nene\n";
    } else {
        std::cout << "moon_star\n";
    }
}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    solve();
}