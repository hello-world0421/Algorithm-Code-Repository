#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(nullptr);
    cout.tie(nullptr);
    ios_base::sync_with_stdio(0);
    
    int n, m;
    cin >> n >> m;
    int a, b;
    for (int i = 0; i < m; i++)
        cin >> a >> b;
    cout << 3;
}