#include <bits/stdc++.h>
using namespace std;

void solve() {
    string n; cin >> n;
    if (n[0] == '1' && n[1] == '0') {
        if (n.size() == 2) {
            
        }
    }
}

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);

    int tt; cin >> tt;
    while (tt--) {
        solve();
    }

    return 0;
}